[
  {
    "type": "index",
    "name": "idx_audit_log_created",
    "sql": "CREATE INDEX idx_audit_log_created ON audit_log(created_at)"
  },
  {
    "type": "index",
    "name": "idx_chest_def_ref_unique",
    "sql": "CREATE UNIQUE INDEX idx_chest_def_ref_unique ON chest_definition_ref(type, name, source, points, COALESCE(override_points, -1))"
  },
  {
    "type": "index",
    "name": "idx_chest_records_captured",
    "sql": "CREATE INDEX idx_chest_records_captured ON chest_records(captured_at)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_chest_id",
    "sql": "CREATE INDEX idx_chest_records_chest_id ON chest_records(chest_id)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_chest_source_id",
    "sql": "CREATE INDEX idx_chest_records_chest_source_id ON chest_records(chest_source_id)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_clan_chest_captured",
    "sql": "CREATE INDEX idx_chest_records_clan_chest_captured ON chest_records(clan_id, chest_id, captured_at)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_clan_points",
    "sql": "CREATE INDEX idx_chest_records_clan_points ON chest_records(clan_id, point_value)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_clan_source_captured",
    "sql": "CREATE INDEX idx_chest_records_clan_source_captured ON chest_records(clan_id, chest_source_id, captured_at)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_clan_source_chest",
    "sql": "CREATE INDEX idx_chest_records_clan_source_chest ON chest_records(clan_id, chest_source_id, chest_id)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_member",
    "sql": "CREATE INDEX idx_chest_records_member ON chest_records(member_id)"
  },
  {
    "type": "index",
    "name": "idx_chest_records_session",
    "sql": "CREATE INDEX idx_chest_records_session ON chest_records(session_id)"
  },
  {
    "type": "index",
    "name": "idx_clans_public_share_token",
    "sql": "CREATE UNIQUE INDEX idx_clans_public_share_token ON clans(public_share_token) WHERE public_share_token != ''"
  },
  {
    "type": "index",
    "name": "idx_cr_clan_captured_member_pts",
    "sql": "CREATE INDEX idx_cr_clan_captured_member_pts ON chest_records(clan_id, captured_at, member_id, point_value)"
  },
  {
    "type": "index",
    "name": "idx_cr_clan_chest_pts",
    "sql": "CREATE INDEX idx_cr_clan_chest_pts ON chest_records(clan_id, chest_id, point_value)"
  },
  {
    "type": "index",
    "name": "idx_cr_clan_effective_member_pts",
    "sql": "CREATE INDEX idx_cr_clan_effective_member_pts ON chest_records(clan_id, effective_at, member_id, point_value)"
  },
  {
    "type": "index",
    "name": "idx_cr_clan_member_captured_pts",
    "sql": "CREATE INDEX idx_cr_clan_member_captured_pts ON chest_records(clan_id, member_id, captured_at, point_value)"
  },
  {
    "type": "index",
    "name": "idx_cr_clan_member_effective_pts",
    "sql": "CREATE INDEX idx_cr_clan_member_effective_pts ON chest_records(clan_id, member_id, effective_at, point_value)"
  },
  {
    "type": "index",
    "name": "idx_cr_clan_source_pts",
    "sql": "CREATE INDEX idx_cr_clan_source_pts ON chest_records(clan_id, chest_source_id, point_value)"
  },
  {
    "type": "index",
    "name": "idx_discord_links_member",
    "sql": "CREATE INDEX idx_discord_links_member ON discord_member_links(member_id)"
  },
  {
    "type": "index",
    "name": "idx_member_snapshots_clan",
    "sql": "CREATE INDEX idx_member_snapshots_clan ON member_snapshots(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_member_snapshots_clan_day",
    "sql": "CREATE INDEX idx_member_snapshots_clan_day ON member_snapshots(clan_id, game_date)"
  },
  {
    "type": "index",
    "name": "idx_member_snapshots_member",
    "sql": "CREATE INDEX idx_member_snapshots_member ON member_snapshots(member_id)"
  },
  {
    "type": "index",
    "name": "idx_member_snapshots_member_day",
    "sql": "CREATE UNIQUE INDEX idx_member_snapshots_member_day ON member_snapshots(member_id, game_date) WHERE game_date != ''"
  },
  {
    "type": "index",
    "name": "idx_members_clan",
    "sql": "CREATE INDEX idx_members_clan ON members(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_members_clan_active",
    "sql": "CREATE INDEX idx_members_clan_active ON members(clan_id, is_active)"
  },
  {
    "type": "index",
    "name": "idx_members_despaced",
    "sql": "CREATE INDEX idx_members_despaced ON members(clan_id, despaced_name)"
  },
  {
    "type": "index",
    "name": "idx_members_name",
    "sql": "CREATE INDEX idx_members_name ON members(name)"
  },
  {
    "type": "index",
    "name": "idx_members_normalized",
    "sql": "CREATE INDEX idx_members_normalized ON members(normalized_name)"
  },
  {
    "type": "index",
    "name": "idx_player_category_ref",
    "sql": "CREATE INDEX idx_player_category_ref ON player_category(player_ref_id)"
  },
  {
    "type": "index",
    "name": "idx_player_category_snapshot",
    "sql": "CREATE INDEX idx_player_category_snapshot ON player_category(snapshot_id)"
  },
  {
    "type": "index",
    "name": "idx_player_snapshot_clan",
    "sql": "CREATE INDEX idx_player_snapshot_clan ON player_snapshot(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_player_snapshot_ref",
    "sql": "CREATE INDEX idx_player_snapshot_ref ON player_snapshot(player_ref_id)"
  },
  {
    "type": "index",
    "name": "idx_player_snapshot_snapshot",
    "sql": "CREATE INDEX idx_player_snapshot_snapshot ON player_snapshot(snapshot_id)"
  },
  {
    "type": "index",
    "name": "idx_poll_log_clan",
    "sql": "CREATE INDEX idx_poll_log_clan ON poll_log(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_poll_log_polled_at",
    "sql": "CREATE INDEX idx_poll_log_polled_at ON poll_log(polled_at)"
  },
  {
    "type": "index",
    "name": "idx_poll_log_share_code",
    "sql": "CREATE INDEX idx_poll_log_share_code ON poll_log(share_code)"
  },
  {
    "type": "index",
    "name": "idx_resource_icon_templates_clan",
    "sql": "CREATE INDEX idx_resource_icon_templates_clan ON resource_icon_templates(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_resource_transactions_clan",
    "sql": "CREATE INDEX idx_resource_transactions_clan ON resource_transactions(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_resource_transactions_date",
    "sql": "CREATE INDEX idx_resource_transactions_date ON resource_transactions(clan_id, transaction_date)"
  },
  {
    "type": "index",
    "name": "idx_resource_transactions_member",
    "sql": "CREATE INDEX idx_resource_transactions_member ON resource_transactions(member_id)"
  },
  {
    "type": "index",
    "name": "idx_resource_transactions_unresolved",
    "sql": "CREATE INDEX idx_resource_transactions_unresolved ON resource_transactions(clan_id, transaction_date) WHERE resource_type_id IS NULL"
  },
  {
    "type": "index",
    "name": "idx_resource_upload_batches_clan",
    "sql": "CREATE INDEX idx_resource_upload_batches_clan ON resource_upload_batches(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_scan_sessions_clan",
    "sql": "CREATE INDEX idx_scan_sessions_clan ON scan_sessions(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_scan_sessions_clan_status_completed",
    "sql": "CREATE INDEX idx_scan_sessions_clan_status_completed ON scan_sessions(clan_id, status, completed_at)"
  },
  {
    "type": "index",
    "name": "idx_share_links_clan",
    "sql": "CREATE INDEX idx_share_links_clan ON share_links(clan_id, revoked_at)"
  },
  {
    "type": "index",
    "name": "idx_share_links_token",
    "sql": "CREATE UNIQUE INDEX idx_share_links_token ON share_links(token)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_chest_def_clan",
    "sql": "CREATE INDEX idx_snapshot_chest_def_clan ON snapshot_chest_definition(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_chest_def_ref",
    "sql": "CREATE INDEX idx_snapshot_chest_def_ref ON snapshot_chest_definition(chest_definition_ref_id)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_clan",
    "sql": "CREATE INDEX idx_snapshot_clan ON snapshot(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_fetched_at",
    "sql": "CREATE INDEX idx_snapshot_fetched_at ON snapshot(fetched_at)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_kingdom",
    "sql": "CREATE INDEX idx_snapshot_kingdom ON snapshot(kingdom)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_last_scanned_at",
    "sql": "CREATE INDEX idx_snapshot_last_scanned_at ON snapshot(last_scanned_at)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_share_code",
    "sql": "CREATE INDEX idx_snapshot_share_code ON snapshot(share_code)"
  },
  {
    "type": "index",
    "name": "idx_snapshot_window",
    "sql": "CREATE INDEX idx_snapshot_window ON snapshot(window_start, window_end)"
  },
  {
    "type": "index",
    "name": "idx_triumphal_chest_records_captured",
    "sql": "CREATE INDEX idx_triumphal_chest_records_captured ON triumphal_chest_records(captured_at)"
  },
  {
    "type": "index",
    "name": "idx_triumphal_chest_records_clan",
    "sql": "CREATE INDEX idx_triumphal_chest_records_clan ON triumphal_chest_records(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_triumphal_chest_records_member",
    "sql": "CREATE INDEX idx_triumphal_chest_records_member ON triumphal_chest_records(member_id)"
  },
  {
    "type": "index",
    "name": "idx_triumphal_chest_records_session",
    "sql": "CREATE INDEX idx_triumphal_chest_records_session ON triumphal_chest_records(session_id)"
  },
  {
    "type": "index",
    "name": "idx_triumphal_clan_effective",
    "sql": "CREATE INDEX idx_triumphal_clan_effective ON triumphal_chest_records(clan_id, effective_at)"
  },
  {
    "type": "index",
    "name": "idx_user_sessions_token",
    "sql": "CREATE INDEX idx_user_sessions_token ON user_sessions(token)"
  },
  {
    "type": "index",
    "name": "idx_users_clan",
    "sql": "CREATE INDEX idx_users_clan ON users(clan_id)"
  },
  {
    "type": "index",
    "name": "idx_users_username",
    "sql": "CREATE INDEX idx_users_username ON users(username)"
  },
  {
    "type": "table",
    "name": "audit_log",
    "sql": "CREATE TABLE audit_log ( id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER REFERENCES users(id), clan_id INTEGER REFERENCES clans(id), action TEXT NOT NULL, details TEXT, created_at TEXT NOT NULL )"
  },
  {
    "type": "table",
    "name": "chest_daily_summary",
    "sql": "CREATE TABLE chest_daily_summary ( clan_id INTEGER NOT NULL, member_id INTEGER NOT NULL, game_day TEXT NOT NULL, chests INTEGER NOT NULL, points INTEGER NOT NULL, earned_chests INTEGER NOT NULL DEFAULT 0, earned_points INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (clan_id, member_id, game_day) )"
  },
  {
    "type": "table",
    "name": "chest_definition_ref",
    "sql": "CREATE TABLE chest_definition_ref ( id INTEGER PRIMARY KEY AUTOINCREMENT, type TEXT NOT NULL, name TEXT NOT NULL, source TEXT NOT NULL, points INTEGER NOT NULL DEFAULT 0, override_points INTEGER )"
  },
  {
    "type": "table",
    "name": "chest_records",
    "sql": "CREATE TABLE chest_records ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL DEFAULT 1, session_id INTEGER NOT NULL REFERENCES scan_sessions(id), member_id INTEGER NOT NULL REFERENCES members(id), chest_id INTEGER NOT NULL REFERENCES chests(id), chest_source_id INTEGER REFERENCES chest_sources(id), point_value INTEGER NOT NULL DEFAULT 0, captured_at INTEGER NOT NULL, confidence INTEGER NOT NULL DEFAULT 0, debug_crop_path TEXT, raw_player_ocr TEXT, earned_at INTEGER, effective_at GENERATED ALWAYS AS (COALESCE(earned_at, captured_at)) VIRTUAL, UNIQUE(session_id, member_id, chest_id, captured_at) )"
  },
  {
    "type": "table",
    "name": "chest_sources",
    "sql": "CREATE TABLE chest_sources ( id INTEGER PRIMARY KEY AUTOINCREMENT, source TEXT NOT NULL UNIQUE )"
  },
  {
    "type": "table",
    "name": "chest_type_overrides",
    "sql": "CREATE TABLE chest_type_overrides ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL REFERENCES clans(id) DEFAULT 1, chest_name TEXT NOT NULL, chest_type TEXT NOT NULL, created_at TEXT NOT NULL, UNIQUE(clan_id, chest_name) )"
  },
  {
    "type": "table",
    "name": "chests",
    "sql": "CREATE TABLE chests ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE, chest_type TEXT NOT NULL DEFAULT 'unknown' )"
  },
  {
    "type": "table",
    "name": "clans",
    "sql": "CREATE TABLE clans ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE, slug TEXT NOT NULL UNIQUE, game_url TEXT NOT NULL, scan_interval_minutes INTEGER, is_active INTEGER NOT NULL DEFAULT 1, created_at TEXT NOT NULL, created_by INTEGER REFERENCES users(id), discord_enabled INTEGER NOT NULL DEFAULT 0, discord_token TEXT NOT NULL DEFAULT '', discord_channel_id TEXT NOT NULL DEFAULT '', discord_guild_id TEXT NOT NULL DEFAULT '', discord_scan_reports_enabled INTEGER NOT NULL DEFAULT 1, discord_only_new_chests INTEGER NOT NULL DEFAULT 0, discord_daily_digest_enabled INTEGER NOT NULL DEFAULT 0, discord_commands_enabled INTEGER NOT NULL DEFAULT 1, ct_share_code TEXT NOT NULL DEFAULT '', ct_poll_interval_hours INTEGER, ct_backfill_weeks INTEGER, public_share_token TEXT NOT NULL DEFAULT '', discord_daily_digest_share_user_id TEXT NOT NULL DEFAULT '', last_digest_at TEXT NOT NULL DEFAULT '', last_digest_channel_error TEXT NOT NULL DEFAULT '', last_digest_dm_error TEXT NOT NULL DEFAULT '' , needs_reauth INTEGER NOT NULL DEFAULT 0, reauth_failed_at TEXT NOT NULL DEFAULT '', resources_enabled INTEGER NOT NULL DEFAULT 0, last_digest_game_day TEXT NOT NULL DEFAULT '', inactivity_days INTEGER, inactivity_sweep_enabled INTEGER NOT NULL DEFAULT 1, resource_auto_capture INTEGER NOT NULL DEFAULT 1, leaderboard_goal_enabled INTEGER NOT NULL DEFAULT 0, leaderboard_weekly_goal_points INTEGER, deleted_at TEXT NOT NULL DEFAULT '')"
  },
  {
    "type": "table",
    "name": "ct_config",
    "sql": "CREATE TABLE ct_config ( key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at TEXT NOT NULL )"
  },
  {
    "type": "table",
    "name": "ct_player_ref",
    "sql": "CREATE TABLE ct_player_ref ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL DEFAULT 1, name TEXT NOT NULL, UNIQUE(clan_id, name) )"
  },
  {
    "type": "table",
    "name": "discord_member_links",
    "sql": "CREATE TABLE discord_member_links ( clan_id INTEGER NOT NULL REFERENCES clans(id), discord_user_id TEXT NOT NULL, member_id INTEGER NOT NULL REFERENCES members(id) ON DELETE CASCADE, linked_at TEXT NOT NULL, PRIMARY KEY (clan_id, discord_user_id) )"
  },
  {
    "type": "table",
    "name": "member_snapshots",
    "sql": "CREATE TABLE member_snapshots ( id INTEGER PRIMARY KEY AUTOINCREMENT, member_id INTEGER NOT NULL REFERENCES members(id), level INTEGER NOT NULL, power INTEGER NOT NULL, captured_at TEXT NOT NULL, clan_id INTEGER DEFAULT 1 , game_date TEXT NOT NULL DEFAULT '', row_crop_path TEXT)"
  },
  {
    "type": "table",
    "name": "members",
    "sql": "CREATE TABLE members ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL REFERENCES clans(id) DEFAULT 1, name TEXT NOT NULL, normalized_name TEXT NOT NULL, aliases TEXT DEFAULT '[]', first_seen TEXT NOT NULL, last_seen TEXT NOT NULL, is_active INTEGER DEFAULT 1, level INTEGER DEFAULT 0, power INTEGER DEFAULT 0, clan_role TEXT DEFAULT '', despaced_name TEXT NOT NULL DEFAULT '', left_at TEXT, UNIQUE(clan_id, normalized_name) )"
  },
  {
    "type": "table",
    "name": "merge_rules",
    "sql": "CREATE TABLE merge_rules ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL REFERENCES clans(id) DEFAULT 1, type TEXT NOT NULL CHECK(type IN ('player', 'chest', 'source')), from_value TEXT NOT NULL, to_value TEXT NOT NULL, created_at TEXT NOT NULL, UNIQUE(clan_id, type, from_value) )"
  },
  {
    "type": "table",
    "name": "player_category",
    "sql": "CREATE TABLE player_category ( id INTEGER PRIMARY KEY AUTOINCREMENT, snapshot_id INTEGER NOT NULL REFERENCES snapshot(id) ON DELETE CASCADE, player_ref_id INTEGER NOT NULL REFERENCES ct_player_ref(id), category TEXT NOT NULL, chests INTEGER NOT NULL DEFAULT 0, clan_id INTEGER NOT NULL DEFAULT 1 )"
  },
  {
    "type": "table",
    "name": "player_snapshot",
    "sql": "CREATE TABLE player_snapshot ( id INTEGER PRIMARY KEY AUTOINCREMENT, snapshot_id INTEGER NOT NULL REFERENCES snapshot(id) ON DELETE CASCADE, player_ref_id INTEGER NOT NULL REFERENCES ct_player_ref(id), guards_level INTEGER NOT NULL DEFAULT 0, points INTEGER NOT NULL DEFAULT 0, chests INTEGER NOT NULL DEFAULT 0, clan_id INTEGER NOT NULL DEFAULT 1 )"
  },
  {
    "type": "table",
    "name": "poll_log",
    "sql": "CREATE TABLE poll_log ( id INTEGER PRIMARY KEY AUTOINCREMENT, polled_at TEXT NOT NULL, share_code TEXT NOT NULL, window_start TEXT NOT NULL, window_end TEXT NOT NULL, trigger TEXT NOT NULL, status INTEGER NOT NULL, etag_changed INTEGER NOT NULL DEFAULT 0, prior_etag TEXT, new_etag TEXT, error_message TEXT, clan_id INTEGER NOT NULL DEFAULT 1 )"
  },
  {
    "type": "table",
    "name": "resource_capture_cursor",
    "sql": "CREATE TABLE resource_capture_cursor ( clan_id INTEGER PRIMARY KEY REFERENCES clans(id), -- JSON array of row fingerprints, newest first, starting at the newest row -- the game had FINISHED writing. Not the top of the list: those rows are -- still being merged into and move. See buildCursorFingerprints in -- browser/resource-sweep-rules.ts. top_rows TEXT NOT NULL, -- Game day the capture last RAN on. Purely the once-a-day gate, and it -- advances even on a run that could not move the marker. game_date TEXT NOT NULL, captured_at TEXT NOT NULL, -- transaction_date of the row top_rows starts at — i.e. how old the MARKER -- is, which is not the same as how long ago the phase ran. The date backstop -- is sized from this, so that a run of unusable reads widens the next sweep -- to reach a marker that is getting older instead of stopping short of it. newest_date TEXT NOT NULL DEFAULT '', rows_inserted INTEGER NOT NULL DEFAULT 0 )"
  },
  {
    "type": "table",
    "name": "resource_icon_templates",
    "sql": "CREATE TABLE resource_icon_templates ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL REFERENCES clans(id), resource_type_id INTEGER NOT NULL REFERENCES resource_types(id), template_data BLOB NOT NULL, updated_at TEXT NOT NULL, UNIQUE(clan_id, resource_type_id) )"
  },
  {
    "type": "table",
    "name": "resource_transactions",
    "sql": "CREATE TABLE resource_transactions ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL REFERENCES clans(id), batch_id INTEGER NOT NULL REFERENCES resource_upload_batches(id), member_id INTEGER NOT NULL REFERENCES members(id), resource_type_id INTEGER REFERENCES resource_types(id), direction INTEGER NOT NULL CHECK(direction IN (1, -1)), amount INTEGER NOT NULL CHECK(amount > 0), transaction_date TEXT NOT NULL, raw_player_name TEXT NOT NULL DEFAULT '', created_at TEXT NOT NULL , row_crop_path TEXT)"
  },
  {
    "type": "table",
    "name": "resource_types",
    "sql": "CREATE TABLE resource_types ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE, slug TEXT NOT NULL UNIQUE )"
  },
  {
    "type": "table",
    "name": "resource_upload_batches",
    "sql": "CREATE TABLE resource_upload_batches ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL REFERENCES clans(id), uploaded_by INTEGER REFERENCES users(id), uploaded_at TEXT NOT NULL, upload_date TEXT NOT NULL, row_count INTEGER NOT NULL DEFAULT 0, error_count INTEGER NOT NULL DEFAULT 0, notes TEXT NOT NULL DEFAULT '', file_count INTEGER NOT NULL DEFAULT 1 , source TEXT NOT NULL DEFAULT 'upload')"
  },
  {
    "type": "table",
    "name": "review_acknowledgments",
    "sql": "CREATE TABLE review_acknowledgments ( clan_id INTEGER NOT NULL REFERENCES clans(id) DEFAULT 1, category TEXT NOT NULL CHECK(category IN ('chest_name', 'chest_source', 'member')), acknowledged_at TEXT NOT NULL, PRIMARY KEY (clan_id, category) )"
  },
  {
    "type": "table",
    "name": "scan_sessions",
    "sql": "CREATE TABLE scan_sessions ( id INTEGER PRIMARY KEY AUTOINCREMENT, started_at TEXT NOT NULL, completed_at TEXT, status TEXT NOT NULL DEFAULT 'pending', chests_found INTEGER DEFAULT 0, screenshots_taken INTEGER DEFAULT 0, errors_encountered INTEGER DEFAULT 0, trigger_source TEXT NOT NULL DEFAULT 'scheduled', error_message TEXT, error_phase TEXT, clan_id INTEGER DEFAULT 1 )"
  },
  {
    "type": "table",
    "name": "schema_version",
    "sql": "CREATE TABLE schema_version ( version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL )"
  },
  {
    "type": "table",
    "name": "share_link_daily",
    "sql": "CREATE TABLE share_link_daily ( link_id INTEGER NOT NULL REFERENCES share_links(id), day TEXT NOT NULL, views INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (link_id, day) )"
  },
  {
    "type": "table",
    "name": "share_links",
    "sql": "CREATE TABLE share_links ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL REFERENCES clans(id), token TEXT NOT NULL, created_at TEXT NOT NULL, created_by INTEGER REFERENCES users(id), revoked_at TEXT, revoked_by INTEGER REFERENCES users(id), revoke_reason TEXT NOT NULL DEFAULT '', hit_count INTEGER NOT NULL DEFAULT 0, last_used_at TEXT, api_hit_count INTEGER NOT NULL DEFAULT 0, api_last_used_at TEXT , unique_visits INTEGER NOT NULL DEFAULT 0, return_visits INTEGER NOT NULL DEFAULT 0, duration_ms_total INTEGER NOT NULL DEFAULT 0, duration_samples INTEGER NOT NULL DEFAULT 0, timeframe_changes INTEGER NOT NULL DEFAULT 0)"
  },
  {
    "type": "table",
    "name": "snapshot",
    "sql": "CREATE TABLE snapshot ( id INTEGER PRIMARY KEY AUTOINCREMENT, fetched_at TEXT NOT NULL, share_code TEXT NOT NULL, window_start TEXT NOT NULL, window_end TEXT NOT NULL, duration_days INTEGER NOT NULL, player_count INTEGER NOT NULL DEFAULT 0, total_chests INTEGER NOT NULL DEFAULT 0, total_points INTEGER NOT NULL DEFAULT 0, trigger TEXT NOT NULL DEFAULT 'scheduled', etag TEXT, settings_json TEXT, last_scanned_at TEXT, kingdom INTEGER, scoring_json TEXT, clan_id INTEGER NOT NULL DEFAULT 1 )"
  },
  {
    "type": "table",
    "name": "snapshot_chest_definition",
    "sql": "CREATE TABLE snapshot_chest_definition ( snapshot_id INTEGER NOT NULL REFERENCES snapshot(id) ON DELETE CASCADE, chest_definition_ref_id INTEGER NOT NULL REFERENCES chest_definition_ref(id), clan_id INTEGER NOT NULL DEFAULT 1, PRIMARY KEY (snapshot_id, chest_definition_ref_id) )"
  },
  {
    "type": "table",
    "name": "source_point_overrides",
    "sql": "CREATE TABLE source_point_overrides ( id INTEGER PRIMARY KEY AUTOINCREMENT, source_key TEXT NOT NULL, chest_name TEXT NOT NULL DEFAULT '', point_value INTEGER NOT NULL, updated_at TEXT NOT NULL, UNIQUE(source_key, chest_name) )"
  },
  {
    "type": "table",
    "name": "triumphal_chest_points",
    "sql": "CREATE TABLE triumphal_chest_points ( id INTEGER PRIMARY KEY AUTOINCREMENT, chest_name TEXT NOT NULL UNIQUE, package_points INTEGER NOT NULL, updated_at TEXT NOT NULL )"
  },
  {
    "type": "table",
    "name": "triumphal_chest_records",
    "sql": "CREATE TABLE triumphal_chest_records ( id INTEGER PRIMARY KEY AUTOINCREMENT, clan_id INTEGER NOT NULL DEFAULT 1, session_id INTEGER NOT NULL REFERENCES scan_sessions(id), member_id INTEGER NOT NULL REFERENCES members(id), chest_id INTEGER NOT NULL REFERENCES chests(id), chest_source_id INTEGER REFERENCES chest_sources(id), point_value INTEGER NOT NULL DEFAULT 0, captured_at INTEGER NOT NULL, confidence INTEGER NOT NULL DEFAULT 0, debug_crop_path TEXT , raw_player_ocr TEXT, earned_at INTEGER, effective_at GENERATED ALWAYS AS (COALESCE(earned_at, captured_at)) VIRTUAL)"
  },
  {
    "type": "table",
    "name": "user_sessions",
    "sql": "CREATE TABLE user_sessions ( id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE, token TEXT NOT NULL UNIQUE, expires_at TEXT NOT NULL, created_at TEXT NOT NULL, active_clan_id INTEGER REFERENCES clans(id) )"
  },
  {
    "type": "table",
    "name": "users",
    "sql": "CREATE TABLE users ( id INTEGER PRIMARY KEY AUTOINCREMENT, username TEXT NOT NULL UNIQUE, password_hash TEXT NOT NULL, role TEXT NOT NULL DEFAULT 'user' CHECK(role IN ('superadmin', 'admin', 'user')), created_by INTEGER REFERENCES users(id), created_at TEXT NOT NULL, last_login TEXT, clan_id INTEGER REFERENCES clans(id) , last_visited TEXT, theme TEXT NOT NULL DEFAULT 'dark')"
  },
  {
    "type": "view",
    "name": "chest_records_v",
    "sql": "CREATE VIEW chest_records_v AS SELECT cr.id, cr.clan_id, cr.session_id, m.name AS player_name, cr.member_id, ch.name AS chest_name, ch.chest_type, COALESCE(cs.source, '') AS chest_source, 1 AS quantity, cr.point_value, cr.captured_at, cr.effective_at, cr.confidence, cr.debug_crop_path, cr.chest_id, cr.chest_source_id FROM chest_records cr JOIN members m ON m.id = cr.member_id JOIN chests ch ON ch.id = cr.chest_id LEFT JOIN chest_sources cs ON cs.id = cr.chest_source_id"
  },
  {
    "type": "view",
    "name": "triumphal_chest_records_v",
    "sql": "CREATE VIEW triumphal_chest_records_v AS SELECT tr.id, tr.clan_id, tr.session_id, m.name AS player_name, tr.member_id, ch.name AS chest_name, ch.chest_type, COALESCE(cs.source, '') AS chest_source, 1 AS quantity, tr.point_value, tr.captured_at, tr.effective_at, tr.confidence, tr.debug_crop_path, tr.chest_id, tr.chest_source_id FROM triumphal_chest_records tr JOIN members m ON m.id = tr.member_id JOIN chests ch ON ch.id = tr.chest_id LEFT JOIN chest_sources cs ON cs.id = tr.chest_source_id"
  }
]
